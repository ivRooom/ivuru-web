name: Format iOS Inline Recovery

on:
  push:
    branches:
      - fix/ios-webkit-story-scroll

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/ios-webkit-story-scroll
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - name: Inline Recovery関連ファイルを整形
        run: |
          npx prettier --write \
            src/components/effects/IOSStoryDirectorRecovery.astro \
            src/components/pages/HomePageV2.astro \
            tests/unit/ios-story-director-recovery-contract.test.ts
      - name: 整形結果をコミット
        run: |
          if git diff --quiet; then
            echo "整形差分はありません。"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "4189822+github-actions[bot]@users.noreply.github.com"
          git add \
            src/components/effects/IOSStoryDirectorRecovery.astro \
            src/components/pages/HomePageV2.astro \
            tests/unit/ios-story-director-recovery-contract.test.ts
          git commit -m "iOS Inline Recovery関連ファイルを整形"
          git push origin HEAD:fix/ios-webkit-story-scroll
