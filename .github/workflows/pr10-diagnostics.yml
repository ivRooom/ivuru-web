name: PR10 diagnostics
on:
  pull_request:
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: 型チェック結果
        run: |
          git clone -q --depth 1 --branch feat/profile-social-clips-developer-hub https://github.com/ivRooom/ivuru-web.git repo
          cd repo
          npm ci --silent >/dev/null
          npm run check
